h1 executions Show
= cell(Domicile::Cell::Flash)
- execution = model.participations.find_by(id: params[:participation_id]).executions.find_by(id: params[:id])
- execution.images.each do |image|
  img src="#{url_for(image)}" style="width: 300px"
